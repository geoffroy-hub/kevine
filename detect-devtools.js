// detect-devtools.js — stub silencieux (remplace la dépendance manquante)
// Désactive toute détection d'outils de développement pour permettre
// le bon fonctionnement du site.
var DisableDevtool = function(options) {
  // Stub : ne fait rien — le site fonctionne normalement.
};
